pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "hello world"'
                //or
        stage('deploy') {
            steps {
                sh '''
                    echo "hello"
                    echo "hello ello" }
                '''
            }
        }
    }
}
