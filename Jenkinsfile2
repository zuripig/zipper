pipeline {
    agent { dockerfile true }

    stages {
        stage('Build') {
            steps {
                sh 'echo building'
            }
        }
        stage('Scan') {
            steps {
                sh 'docker scan f7f7557adf2d38276ead3d72bf63e3bfe9df8e2c'
            }
        }
        stage('Artifact') {
            steps {
                sh 'echo artifacting'
            }
        }
    }
}
